import 'package:flutter/material.dart';

class AppColors {
  static const MaterialColor primary = Colors.deepOrange; // Example: Orange from images
  static const Color secondary = Color(0xFF6A1B9A); // Example: Purple from timer screen
  static const Color background = Color(0xFFFFF5EE); // Example: Light beige/pinkish background
  static const Color cardBackground = Colors.deepOrangeAccent; // For the card in input screen
  static const Color textColor = Colors.black87;
  static const Color lightTextColor = Colors.white70;
  static const Color white = Colors.white;
  static const Color accentColor = Colors.amber; // For highlights or buttons
}